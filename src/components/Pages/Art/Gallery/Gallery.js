import React from 'react';
import Aux from '../../../../hoc/Aux';
import classes from './Gallery.module.css';

const gallery = (props) => (
    <div className={classes.Gallery}>
        <p className={classes.Title}>Art Gallery</p>
    </div>
)

export default gallery;